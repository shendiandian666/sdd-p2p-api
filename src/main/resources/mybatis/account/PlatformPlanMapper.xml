<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sdd.mapper.PlatformPlanMapper">

	<select id="listByPlan" parameterType="string" resultType="java.util.Map">
		SELECT * from p2p_platform_plan 
		where plat_id=#{id}
	</select>
	
    
    
   
</mapper>